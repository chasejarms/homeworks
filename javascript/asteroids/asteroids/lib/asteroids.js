const Game = require('./game_view.js');

document.addEventListener(
  "DOMContentLoaded", function() {
    let canvasEl = document.getElementsByTagName("canvas")[0];
    let ctx = canvasEl.getContext('2D');
  });
