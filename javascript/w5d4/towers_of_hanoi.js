class TowersOfHanoi {
  constructor() {
    this.game = new Game();
  }
  run(this.game, callback) {
    // prompt user for move


    let gameOver = false;
    if (gameOver) {
      console.log("Congrats, you won!");
    } else {
      callback(this.game, run)
    }
    // make move
    // end game if game is won
    // congratulate winner
  }
}

const Game = require("./game.js");

let ourGame = new Game();
console.log(ourGame.towers);
